{% extends 'layout_admin.html.twig' %}

{% block title %}Modifier un projet{% endblock %}

{% block body %}
    <h2 class="title m-3">Modifier le projet : {{ projet.nom }}</h2>

    <div class="container my-5">
        {{ include('projet/_form.html.twig', {'button_label': 'Update'}) }}

        {{ include('projet/_delete_form.html.twig') }}
    </div>

    <a href="{{ path('projet_index') }}" class="btn bouton-retour m-3">Retour à la liste des projets</a>


{% endblock %}
