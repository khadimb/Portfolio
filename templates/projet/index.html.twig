{% extends 'layout_admin.html.twig' %}

{% block title %}Projet index{% endblock %}

{% block body %}
    <h1>Projet index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Lien</th>
                <th>Description</th>
                <th>Catégorie</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for projet in projets %}
            <tr>
                <td>{{ projet.Nom }}</td>
                <td>{{ projet.Lien }}</td>
                <td>{{ projet.description }}</td>
                <td>{{ projet.categorie.nom }}</td>
                <td>
                    <a href="{{ path('projet_show', {'id': projet.id}) }}" class="btn btn-primary m-1">Afficher</a>
                    <a href="{{ path('projet_edit', {'id': projet.id}) }}" class="btn btn-success m-1">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">Aucun Projet</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('projet_new') }}" class="btn btn-success m-3">Ajouter un nouveau projet</a>
{% endblock %}
