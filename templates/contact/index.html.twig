{% extends "layout.html.twig" %}

{% block title %}Contact
{% endblock %}
{% block body %}
	<div class="container min-vh-100">
		<h1>Contact</h1>
		<div class="row d-flex justify-content-center">
			<div class="col-8 border border-1 border-dark shadow mb-5 m-3 p-3">
				{{ form_start(contact) }}
				{{ form_widget(contact) }}
				<div class="d-flex justify-content-end">
					<input type="submit" value="Envoyer" class="btn btn-success mt-3 d-flex justify-content-end">
				</div>
				{{ form_end(contact) }}
			</div>
		</div>
	</div>
{% endblock %}